[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback: let React Router handle all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
